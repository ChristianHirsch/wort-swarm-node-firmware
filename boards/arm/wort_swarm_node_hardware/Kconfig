# Kconfig - wort_swarm_node_hardware

if BOARD_WORT_SWARM_NODE_HARDWARE

config BOARD_ENABLE_DCDC
        bool "Enable DCDC mode"
        select SOC_DCDC_NRF52X
        default y

endif # BOARD_WORT_SWARM_NODE_HARDWARE
